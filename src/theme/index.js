import {DefaultTheme} from '@react-navigation/native'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    background: '#FFFF',
  },
}

export const colors = {
  primary: '#000080',
  secondary: '#B2B2B2',
  accent: '#FFFFFF',
}
